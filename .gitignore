.DS_Store
*.log
node_modules/
dist/
build/
.env
.env.local

# Admin credentials — never commit
admin/config.php

# Auto-generated backup files — never commit
# .htaccess and index.html inside data/backups/ are intentionally tracked
data/backups/*.json
/backups/
